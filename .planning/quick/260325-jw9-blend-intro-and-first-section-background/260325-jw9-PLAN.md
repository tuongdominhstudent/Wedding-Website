---
phase: quick
plan: 260325-jw9
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/sections/intro/IntroSection.module.css
  - client/src/sections/firsts/FirstsJourneySection.module.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Scrolling from intro to firsts section feels like one continuous scene, not two distinct screens"
    - "The DarkVeil background effect in the intro does not create a hard edge at the section boundary"
    - "The firsts section top blends seamlessly with the intro section bottom"
  artifacts:
    - path: "client/src/sections/intro/IntroSection.module.css"
      provides: "Updated intro section with bottom edge that bleeds into the next section"
    - path: "client/src/sections/firsts/FirstsJourneySection.module.css"
      provides: "Updated firsts section with top edge that matches intro bottom"
  key_links:
    - from: "client/src/sections/intro/IntroSection.module.css"
      to: "client/src/sections/firsts/FirstsJourneySection.module.css"
      via: "Matching gradient boundary colors"
      pattern: "#2d000f|#3a0817"
---

<objective>
Blend the intro and firsts section backgrounds so scrolling between them feels like a smooth, continuous scene rather than two distinct screens with a visible seam.

Purpose: Currently the intro ends at ~#2d000f and the firsts begins at #3a0817, creating a noticeable color jump. The DarkVeil shader overlay in the intro also has a hard rectangular boundary. Both need softening so the transition is imperceptible.

Output: Updated CSS for both sections with harmonized gradient boundaries and a fade-out on the intro's bottom edge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@client/src/sections/intro/IntroSection.module.css
@client/src/sections/intro/IntroSection.jsx
@client/src/sections/firsts/FirstsJourneySection.module.css
@client/src/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harmonize section boundary gradients and fade DarkVeil edge</name>
  <files>client/src/sections/intro/IntroSection.module.css, client/src/sections/firsts/FirstsJourneySection.module.css</files>
  <action>
The goal is a seamless color flow from intro bottom into firsts top. Currently:
- Intro: `linear-gradient(180deg, #2a0010 0%, #3a0214 52%, #2d000f 100%)` — ends at #2d000f
- Firsts: `linear-gradient(180deg, #3a0817 0%, #300411 52%, #22000d 100%)` — starts at #3a0817

Changes needed:

1. **IntroSection.module.css — `.introSection` background:**
   - Change the gradient's bottom stop (100%) from `#2d000f` to `#3a0817` so the intro's bottom color exactly matches the firsts' top color. The full gradient becomes:
     `linear-gradient(180deg, #2a0010 0%, #3a0214 52%, #3a0817 100%)`

2. **IntroSection.module.css — `.backgroundEffect` (DarkVeil container):**
   - Add a CSS mask that fades the DarkVeil shader to transparent at the bottom, preventing a hard rectangular edge. Use:
     `mask-image: linear-gradient(to bottom, black 0%, black 70%, transparent 100%);`
     `-webkit-mask-image: linear-gradient(to bottom, black 0%, black 70%, transparent 100%);`
   - This lets the underlying section gradient show through at the bottom, creating a smooth blend into the next section.

3. **FirstsJourneySection.module.css — `.section` background:**
   - Keep the top color at `#3a0817` (already matches after step 1).
   - Optionally add a subtle `padding-top: 0` or verify there is no gap/margin between sections causing a visible line.

Do NOT change any gradient colors in the middle or top of either section — only the boundary zone matters. Do NOT touch any JS files. Use only CSS changes.
  </action>
  <verify>
    <automated>cd /Users/minhtuong03/Documents/Code/Wedding\ card/client && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - The intro gradient bottom stop matches the firsts gradient top stop exactly (#3a0817)
    - The DarkVeil `.backgroundEffect` has a mask-image that fades to transparent at the bottom
    - Build succeeds with no errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Blended background transition between intro and firsts sections by harmonizing gradient boundary colors and adding a fade mask to the DarkVeil shader overlay.</what-built>
  <how-to-verify>
    1. Run `cd client && npm run dev`
    2. Open http://localhost:5173 in the browser
    3. Complete the intro sequence (click the escape/continue button or wait for the video to finish)
    4. Scroll down from the intro section into the firsts ("Nhung cai dau tien") section
    5. Verify: The background should flow smoothly from one section to the next with no visible color jump or hard edge
    6. Check on mobile viewport (use DevTools responsive mode ~375px width) — same smooth transition expected
  </how-to-verify>
  <resume-signal>Type "approved" if the transition is smooth, or describe any remaining visible seam/jump</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Visual inspection confirms no color seam between intro and firsts sections
</verification>

<success_criteria>
Scrolling from intro to firsts feels like one continuous dark scene. No visible color jump, no hard DarkVeil edge at the bottom of the intro section.
</success_criteria>

<output>
After completion, create `.planning/quick/260325-jw9-blend-intro-and-first-section-background/260325-jw9-SUMMARY.md`
</output>
