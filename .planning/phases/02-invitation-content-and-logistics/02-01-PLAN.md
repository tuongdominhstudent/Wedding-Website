---
phase: 02-invitation-content-and-logistics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/config/sectionRegistry.js
  - client/src/hooks/useAppBootstrap.js
  - client/src/sections/StoryRoot.jsx
  - client/src/sections/prewedding/PreweddingSection.jsx
  - client/src/sections/prewedding/PreweddingSection.module.css
  - client/src/sections/prewedding/preweddingAssets.js
  - client/src/sections/calendar/CalendarSection.jsx
  - client/src/sections/calendar/CalendarSection.module.css
  - client/src/sections/calendar/calendarAssets.js
  - client/src/sections/calendar/calendar.constants.js
  - client/src/test/StoryRoot.test.jsx
  - client/src/test/useAppBootstrap.test.jsx
  - client/src/test/PreweddingSection.test.jsx
  - client/src/test/CalendarSection.test.jsx
autonomous: true
requirements:
  - STORY-03
  - STORY-04
user_setup: []
must_haves:
  truths:
    - Guests can move from the current `long-distance` ending into a dedicated prewedding section that uses `preweddingVideo.webm` without expanding the intro boot budget.
    - Guests can view a separate wedding-photo/calendar section that clearly surfaces the wedding date and functions as the bridge from cinematic story content into later logistics.
    - The new Phase 2 story sections are added through the existing section-registry/bootstrap seams so later invitation sections keep composition and preload behavior consistent.
  artifacts:
    - client/src/sections/prewedding/PreweddingSection.jsx
    - client/src/sections/prewedding/preweddingAssets.js
    - client/src/sections/calendar/CalendarSection.jsx
    - client/src/sections/calendar/calendarAssets.js
    - client/src/config/sectionRegistry.js
    - client/src/hooks/useAppBootstrap.js
    - client/src/sections/StoryRoot.jsx
    - client/src/test/PreweddingSection.test.jsx
    - client/src/test/CalendarSection.test.jsx
  key_links:
    - `client/src/config/sectionRegistry.js`, `client/src/hooks/useAppBootstrap.js`, and `client/src/sections/StoryRoot.jsx` remain the shared seams for ordering and deferred asset loading.
    - `client/src/sections/prewedding/PreweddingSection.jsx` delivers the media-led chapter immediately after `long-distance`.
    - `client/src/sections/calendar/CalendarSection.jsx` carries the wedding-date bridge into later practical invitation content.
---

<objective>
Deliver the story-facing half of Phase 2 by adding a dedicated prewedding video section and a separate wedding-photo/calendar bridge section through the shared story registry and deferred-asset architecture.

Purpose: Complete the cinematic invitation flow before the practical logistics sections, while keeping section order, preload discipline, and future editability aligned with the project’s mobile-first story architecture.
Output: Prewedding section, calendar/date section, deferred asset wiring, and regression coverage for section order and section-local behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-invitation-content-and-logistics/02-CONTEXT.md
@.planning/phases/02-invitation-content-and-logistics/02-RESEARCH.md
@.planning/phases/02-invitation-content-and-logistics/02-VALIDATION.md
@client/src/config/sectionRegistry.js
@client/src/hooks/useAppBootstrap.js
@client/src/sections/StoryRoot.jsx
@client/src/sections/distance/LongDistanceJourneySection.jsx
@client/src/test/StoryRoot.test.jsx
@client/src/test/useAppBootstrap.test.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize the dedicated prewedding section through the shared section seam</name>
  <files>client/src/sections/prewedding/PreweddingSection.jsx, client/src/sections/prewedding/PreweddingSection.module.css, client/src/sections/prewedding/preweddingAssets.js, client/src/config/sectionRegistry.js, client/src/hooks/useAppBootstrap.js, client/src/sections/StoryRoot.jsx</files>
  <action>Implement the dedicated prewedding section immediately after `long-distance` using `preweddingVideo.webm`, and wire it into the existing registry/bootstrap/story composition seam rather than mounting it ad hoc. Keep the section mobile-first, video-led, and visually consistent with the cinematic story flow, but ensure the video asset stays deferred and uses viewport-aware playback so it does not regress intro responsiveness or keep decoding unnecessarily offscreen.</action>
  <verify>npm --prefix client run test -- --run --pool=threads</verify>
  <done>The prewedding section renders after `long-distance`, uses the intended asset, remains deferred in bootstrap, and behaves predictably on mobile viewport entry/exit.</done>
</task>

<task type="auto">
  <name>Task 2: Add the wedding-photo/calendar bridge section</name>
  <files>client/src/sections/calendar/CalendarSection.jsx, client/src/sections/calendar/CalendarSection.module.css, client/src/sections/calendar/calendarAssets.js, client/src/sections/calendar/calendar.constants.js, client/src/config/sectionRegistry.js, client/src/sections/StoryRoot.jsx</files>
  <action>Create a separate calendar/date photo section that follows prewedding and functions as the bridge into later logistics content. Make the wedding date visually obvious, keep the section more editorial than logistical, and avoid duplicating the full practical venue information that belongs to the next plan. Organize any copy/media data in section-local constants/assets files so future content edits remain easy.</action>
  <verify>npm --prefix client run test -- --run</verify>
  <done>The story flow now includes a distinct calendar/photo section with the wedding date clearly communicated, and it reads as a transition into practical invitation details rather than a duplicate logistics block.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for story order and deferred Phase 2 media</name>
  <files>client/src/test/StoryRoot.test.jsx, client/src/test/useAppBootstrap.test.jsx, client/src/test/PreweddingSection.test.jsx, client/src/test/CalendarSection.test.jsx</files>
  <action>Extend test coverage so the registry/story composition proves `prewedding` and `calendar` appear in the intended order after intro and `long-distance`, and bootstrap still treats their media as deferred instead of expanding the blocking queue. Add focused component tests for prewedding video behavior and for the presence of the calendar/date bridge content, preferring deterministic assertions over visual snapshots.</action>
  <verify>npm --prefix client run test -- --run</verify>
  <done>Automated tests fail if Phase 2 story sections break ordering, stop rendering, or accidentally become blocking boot assets.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix client run test -- --run`
- [ ] `npm --prefix client run build`
- [ ] Mobile smoke test confirms `long-distance -> prewedding -> calendar` feels like one intentional story progression and the prewedding video still behaves smoothly on entry/exit
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The invitation includes a dedicated prewedding section using `preweddingVideo.webm`
- The invitation includes a wedding-photo/calendar bridge section that clearly surfaces the wedding date
- New Phase 2 story sections preserve deferred loading and shared registry-based composition
</success_criteria>

<output>
After completion, create `.planning/phases/02-invitation-content-and-logistics/02-01-SUMMARY.md`
</output>
