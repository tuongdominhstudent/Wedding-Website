---
phase: 01-boot-and-story-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/package.json
  - client/package-lock.json
  - client/vitest.config.js
  - client/src/test/setup.js
  - client/src/test/useAppBootstrap.test.jsx
  - client/src/test/assetLoader.test.js
  - client/src/hooks/useAppBootstrap.js
  - client/src/services/assets/assetRegistry.js
  - client/src/services/assets/assetLoader.js
  - client/src/sections/intro/introAssets.js
  - client/src/sections/firsts/firstsJourneyAssets.js
  - client/src/sections/distance/longDistanceJourneyAssets.js
  - client/src/stores/useAppStore.js
  - client/src/config/preloaderConfig.js
  - client/src/config/sectionRegistry.js
autonomous: true
requirements:
  - QUAL-02
  - STORY-02
user_setup: []
must_haves:
  truths:
    - Boot reaches `BOOT_PHASE.READY` after intro-critical preload completes instead of waiting on firsts and long-distance assets.
    - Deferred story assets can begin after boot readiness without regressing the initial branded loading flow.
    - The currently implemented story flow keeps working on mobile through a durable section/preload contract that additional v1 sections can adopt without re-coupling bootstrap.
    - The client has a runnable automated test setup that covers bootstrap gating behavior and asset-tier loading.
  artifacts:
    - client/package.json
    - client/package-lock.json
    - client/vitest.config.js
    - client/src/test/setup.js
    - client/src/test/useAppBootstrap.test.jsx
    - client/src/test/assetLoader.test.js
    - client/src/hooks/useAppBootstrap.js
    - client/src/stores/useAppStore.js
    - client/src/config/sectionRegistry.js
  key_links:
    - `client/src/hooks/useAppBootstrap.js` uses tiered asset registration/loading from `client/src/services/assets/assetRegistry.js` and `client/src/services/assets/assetLoader.js`.
    - `client/src/stores/useAppStore.js` exposes the bootstrap/deferred-loading state that the loader and UI can share.
    - `client/src/config/sectionRegistry.js` and the section asset modules define the onboarding contract each future section must follow for blocking vs deferred assets.
    - The new Vitest setup exercises the real bootstrap seam rather than only isolated utility code.
---

<objective>
Harden app bootstrap so initial entry only waits on intro-critical media, while establishing the missing client-side test harness needed for Phase 1 regression protection.

Purpose: Reduce first-entry latency before additional sections are added and create automated coverage for the boot flow that Phase 1 depends on.
Output: Tiered preload/bootstrap implementation plus a working Vitest + RTL setup with bootstrap-focused tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-boot-and-story-hardening/01-CONTEXT.md
@.planning/phases/01-boot-and-story-hardening/01-RESEARCH.md
@.planning/phases/01-boot-and-story-hardening/01-VALIDATION.md
@client/package.json
@client/src/hooks/useAppBootstrap.js
@client/src/services/assets/assetRegistry.js
@client/src/services/assets/assetLoader.js
@client/src/sections/intro/introAssets.js
@client/src/sections/firsts/firstsJourneyAssets.js
@client/src/sections/distance/longDistanceJourneyAssets.js
@client/src/stores/useAppStore.js
@client/src/config/preloaderConfig.js
@client/src/config/sectionRegistry.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 1 test foundation in client</name>
  <files>client/package.json, client/package-lock.json, client/vitest.config.js, client/src/test/setup.js</files>
  <action>Add a minimal Vitest + jsdom + React Testing Library setup under `client/` so Phase 1 can verify intro/bootstrap behavior automatically. Keep scripts non-watch and repo-local, add only the dependencies called for in `01-VALIDATION.md`, and commit the matching `client/package-lock.json` update so the install state is reproducible rather than implied.</action>
  <verify>npm --prefix client run test -- --run --pool=threads</verify>
  <done>`client/package.json` includes a `test` script, `client/package-lock.json` reflects the added dev dependencies, Vitest config exists, shared setup exists, and the command runs in one-shot mode.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor bootstrap into blocking and deferred preload tiers with a section contract</name>
  <files>client/src/hooks/useAppBootstrap.js, client/src/services/assets/assetRegistry.js, client/src/services/assets/assetLoader.js, client/src/sections/intro/introAssets.js, client/src/sections/firsts/firstsJourneyAssets.js, client/src/sections/distance/longDistanceJourneyAssets.js, client/src/stores/useAppStore.js, client/src/config/preloaderConfig.js, client/src/config/sectionRegistry.js</files>
  <action>Change the current all-assets preload path so only intro-critical assets gate `BOOT_PHASE.READY`, while firsts and long-distance assets are registered and loaded through an explicit deferred path after boot or on approach. At the same time, define a durable onboarding/config contract in `sectionRegistry` and the section asset definitions so additional v1 sections can declare order, mount behavior, and blocking-versus-deferred media expectations without editing bootstrap ad hoc each time. Preserve the branded loading screen contract in the app store, keep the implementation inside the existing bootstrap/asset services, and do not create a second competing preload state model or broad new feature surface.</action>
  <verify>npm --prefix client run test -- --run</verify>
  <done>`useAppBootstrap` no longer blocks on firsts and long-distance asset completion, the app store exposes the state needed for deferred loading, and section registration/assets expose a stable contract future sections can adopt without re-wiring bootstrap.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for scoped bootstrap behavior</name>
  <files>client/src/test/useAppBootstrap.test.jsx, client/src/test/assetLoader.test.js</files>
  <action>Write focused tests that prove intro-critical assets alone can advance boot readiness, deferred assets do not block the ready transition, and the asset loader still reports deterministic progress. Include assertions that the section/preload contract rejects or surfaces invalid blocking metadata so future section additions cannot silently reintroduce global preload coupling. Prefer testing the real hook/service seams over brittle implementation snapshots, and include enough mocking to force slow or deferred asset scenarios without depending on network or media playback.</action>
  <verify>npm --prefix client run test -- --run --pool=threads</verify>
  <done>Automated tests fail against the old all-blocking behavior and pass once tiered bootstrap plus the section contract are implemented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix client run test -- --run --pool=threads`
- [ ] `npm --prefix client run build`
- [ ] Fresh-load boot reaches ready state without firsts/long-distance assets being part of the blocking queue, and the section contract clearly defines how future sections join the story without expanding the blocking queue
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Initial entry is gated only by intro-critical preload work
- Additional v1 sections have a documented-in-code contract for ordering and preload tier assignment
- Automated coverage exists for bootstrap tiering, progress behavior, and contract enforcement
  </success_criteria>

<output>
After completion, create `.planning/phases/01-boot-and-story-hardening/01-01-SUMMARY.md`
</output>
