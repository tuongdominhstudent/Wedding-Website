---
phase: 01-boot-and-story-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - client/src/sections/intro/IntroSection.jsx
  - client/src/sections/intro/IntroSection.module.css
  - client/src/sections/intro/intro.constants.js
  - client/src/sections/StoryRoot.jsx
  - client/src/sections/StoryRoot.module.css
  - client/src/stores/useAppStore.js
  - client/src/components/loading/LoadingScreen.jsx
  - client/src/test/IntroSection.test.jsx
  - client/src/test/StoryRoot.test.jsx
autonomous: true
requirements:
  - STORY-01
  - STORY-02
  - QUAL-01
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - Guests can exit the intro through successful playback, explicit romantic escape CTA, or failure fallback without getting trapped.
    - Scroll, document overflow, touch action, and Lenis always recover after any intro exit path.
    - Guests can move from the intro into the currently implemented story flow on mobile, and that flow remains compatible with additional v1 sections added later.
  artifacts:
    - client/src/sections/intro/IntroSection.jsx
    - client/src/sections/StoryRoot.jsx
    - client/src/stores/useAppStore.js
    - client/src/test/IntroSection.test.jsx
    - client/src/test/StoryRoot.test.jsx
  key_links:
    - `client/src/sections/intro/IntroSection.jsx` routes autoplay success, manual escape, and failure fallback through one shared exit/cleanup path.
    - `client/src/sections/StoryRoot.jsx` unlocks the currently implemented downstream sections from intro exit state rather than only `video ended`, while preserving a composition seam for future sections.
    - Automated tests assert the handshake between intro exit and story unlock, including cleanup behavior.
---

<objective>
Make the opening sequence reliably escapable and connect intro exit to story unlock so the invitation remains usable on mobile even when autoplay or reveal behavior misfires.

Purpose: Remove the current single-success-path deadlock risk while preserving the cinematic intro, the current story flow, and the composition seam needed for later v1 sections.
Output: Intro fallback/escape flow, unified cleanup and unlock handshake, and targeted regression tests for exit paths.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-boot-and-story-hardening/01-CONTEXT.md
@.planning/phases/01-boot-and-story-hardening/01-RESEARCH.md
@.planning/phases/01-boot-and-story-hardening/01-VALIDATION.md
@client/src/sections/intro/IntroSection.jsx
@client/src/sections/intro/IntroSection.module.css
@client/src/sections/intro/intro.constants.js
@client/src/sections/StoryRoot.jsx
@client/src/sections/StoryRoot.module.css
@client/src/stores/useAppStore.js
@client/src/components/loading/LoadingScreen.jsx
@client/src/test/useAppBootstrap.test.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bounded intro escape and polished fallback states</name>
  <files>client/src/sections/intro/IntroSection.jsx, client/src/sections/intro/IntroSection.module.css, client/src/sections/intro/intro.constants.js, client/src/stores/useAppStore.js</files>
  <action>Refactor `IntroSection` into explicit progression states so autoplay rejection, video error, and manual escape all resolve through one exit path. Add a delayed romantic escape CTA, degrade hard playback failure into a still/final-frame treatment, and centralize Lenis/document-lock cleanup so no path can leave the page frozen. Keep the current cinematic visual language instead of replacing the section with utilitarian fallback UI.</action>
  <verify>npm --prefix client run test -- --run --pool=threads IntroSection</verify>
  <done>The intro exposes a short-delay escape CTA, has a polished failure state, and guarantees scroll cleanup and exit completion for success, escape, and failure paths.</done>
</task>

<task type="auto">
  <name>Task 2: Replace single intro-complete gate with a story unlock handshake</name>
  <files>client/src/sections/StoryRoot.jsx, client/src/sections/StoryRoot.module.css, client/src/stores/useAppStore.js, client/src/components/loading/LoadingScreen.jsx</files>
  <action>Update the story composition so the currently implemented downstream sections unlock from a broader intro-exit state instead of only `onSequenceComplete` after video end. Preserve the existing section order, keep the intro visible long enough for a natural handoff into the first story section, and route composition through the section/config contract established in Plan `01-01` so additional v1 sections can be added later without rewriting the intro gate or bootstrap assumptions.</action>
  <verify>npm --prefix client run test -- --run --pool=threads StoryRoot</verify>
  <done>`StoryRoot` mounts firsts and long-distance as soon as intro exit resolves, without depending on a single reveal-success callback, without abrupt scroll snapping, and without hard-coding a brittle composition path for future sections.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for intro exit and story unlock</name>
  <files>client/src/test/IntroSection.test.jsx, client/src/test/StoryRoot.test.jsx</files>
  <action>Write targeted component/integration tests for autoplay success, autoplay failure or video error, and delayed manual escape. Assert the shared cleanup path restores scroll-related state and that `StoryRoot` unlocks the currently implemented story flow after each supported exit path while remaining tolerant of later section additions through the new config contract. Prefer deterministic event simulation over timeline snapshots, and cover the failure modes called out in `01-RESEARCH.md` and `01-VALIDATION.md`.</action>
  <verify>npm --prefix client run test -- --run</verify>
  <done>Automated tests cover all three required intro exit journeys and verify story unlock, cleanup behavior, and compatibility with the current section composition contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix client run test -- --run`
- [ ] `npm --prefix client run build`
- [ ] Manual mobile smoke on a fresh visit confirms autoplay success, autoplay failure/fallback, and delayed escape CTA all reach the currently implemented story flow without deadlock
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Intro exit is reliable through success, fallback, and manual escape
- The currently implemented story flow remains smooth and usable on mobile after every intro exit path
- Story unlock remains compatible with additional v1 sections through the shared composition contract from Plan `01-01`
  </success_criteria>

<output>
After completion, create `.planning/phases/01-boot-and-story-hardening/01-02-SUMMARY.md`
</output>
